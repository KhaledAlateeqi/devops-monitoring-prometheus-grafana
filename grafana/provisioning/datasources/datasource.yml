# Auto-provisions Prometheus as a Grafana data source on startup,
# so you don't have to add it manually through the UI.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
